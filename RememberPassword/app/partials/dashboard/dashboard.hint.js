(function () {

    "use strict";

    // Define enroll controller
    var HintController = function (constants, passwordService) {

        var hint = this;
    };

    // Define enroll module
    angular.module("dashboardModule")

    // Enroll controller
    .controller("HintController", ["constants", "secretservice", HintController]);
}());
