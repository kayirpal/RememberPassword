(function () {

    "use strict";

    // Define enroll controller
    var IconController = function (constants, passwordService) {

        var icon = this;
    };

    // Define enroll module
    angular.module("dashboardModule")

    // Enroll controller
    .controller("IconController", ["constants", "secretservice", IconController]);
}());
