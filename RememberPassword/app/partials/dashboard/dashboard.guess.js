(function () {

    "use strict";

    // Define enroll controller
    var GuessController = function (constants, passwordService) {

        var guess = this;
    };

    // Define enroll module
    angular.module("dashboardModule")

    // Enroll controller
    .controller("GuessController", ["constants", "passwordservice", GuessController]);

}());
